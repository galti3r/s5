name: Benchmark

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUST_TOOLCHAIN: "stable"

# Cancel in-progress runs on same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write

jobs:
  # ===========================================================================
  # Benchmark: criterion benchmarks with regression detection
  # ===========================================================================
  benchmark:
    name: Performance Regression Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust ${{ env.RUST_TOOLCHAIN }}
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}

      - name: Cache cargo
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-bench-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-bench-

      - name: Restore benchmark history
        uses: actions/cache@v4
        with:
          path: ./cache
          key: benchmark-data-${{ github.ref_name }}
          restore-keys: |
            benchmark-data-main
            benchmark-data-

      - name: Compile benchmarks
        run: cargo bench --bench acl_bench --bench password_bench --bench config_bench --bench socks5_bench --no-run

      - name: Run benchmarks
        run: |
          mkdir -p cache
          cargo bench --bench acl_bench --bench password_bench --bench config_bench --bench socks5_bench \
            -- --output-format bencher 2>&1 | grep '^test ' | tee bench-output.txt

      - name: Store benchmark results
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: s5 Criterion Benchmarks
          tool: cargo
          output-file-path: bench-output.txt
          auto-push: false
          external-data-json-path: ./cache/benchmark-data.json
          # Comment on PRs with comparison
          comment-on-alert: true
          comment-always: ${{ github.event_name == 'pull_request' }}
          # Alert if regression exceeds 10%
          alert-threshold: "110%"
          # Fail PR if regression detected
          fail-on-alert: ${{ github.event_name == 'pull_request' }}
          alert-comment-cc-users: "@${{ github.repository_owner }}"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          summary-always: true
